package id.my.okisulton.mapsSdk.misc

/**
 *Created by osalimi on 07-02-2022.
 **/
class MyGeoJson {
    /**
    // GEOJSON
    val layer = GeoJsonLayer(mMap, R.raw.map, this)
    layer.addLayerToMap()

    // Add style GeoJson
    val polygonStyle = layer.defaultPolygonStyle
    polygonStyle.apply {
    fillColor = ContextCompat.getColor(this@MapsActivity, R.color.teal_200)
    }

    // Add GeoJson Click listener
    layer.setOnFeatureClickListener {
    Log.d("TAG", "onMapReady: ${it.getProperty("region")}")
    }

    for (feature in layer.features){
    if (feature.hasProperty("region")){
    Log.d("forLoop", "onPropertyReady: ")
    }
    }
     **/
}